function getIndexToIns(arr, num) {
    // Find num place in this sorted array.
  let arr2 = arr.sort((a,b)=> a-b);
   for (var i = 0; i < arr2.length; i++){
     if (arr2[i] >= num){
       
      
     }
   }
   console.log(i);
   
   
  }
  
   